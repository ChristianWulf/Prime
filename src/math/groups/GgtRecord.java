package math.groups;

public class GgtRecord {

	public int	a, b, c;
	public int	result;

	@Override
	public String toString() {
		return "ggT(" + a + "," + b + ") = " + result + "(" + c + ")";
	}
}
